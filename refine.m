function meshDataNew = refine(meshData, solutionData, adaptivityData)
    % The error indicators for each element.
    errorIndicators = zeros(meshData.no_elements);
    
    % Loops over elements to calculate elemental indicators.
    for i = 1:meshData.no_elements
        residual = 
    end
    
    
    
    meshDataNew = meshData;
end